import {makeStyles} from '@material-ui/styles';
const useStyles = makeStyles({
    articleSummaryContainer: {
        marginRight: 24,
    },
    articleSummaryContainerMobile: {
        marginLeft: 16,
        marginRight: 16,
        marginBottom: 40,
    },
});

export default useStyles;
